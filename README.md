# The Fastify Workshop

## Requirements

1. Node LTS
2. docker-compose

## Setup

- `npm i`
- `npm run db:up`

## Running the modules

- `cd src/step-{n}`
- follow the instructions in the step folder's README
